using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(HingeJoint2D), typeof(DistanceJoint2D), typeof(Rigidbody2D)) ]

public class ItemForRope : MonoBehaviour
{
    // Start is called before the first frame update


}
